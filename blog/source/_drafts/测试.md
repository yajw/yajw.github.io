单元测试，推动模块化

## 难以测试的代码

- 大方法
- 逻辑不够易懂，例如啰嗦或者杂乱，hack味道


#### Case SMID

一个向第三方注册商家的方法，100行，包含了多个依赖，还包含了步骤：查询需要处理的批次号 -> 从第三方sftp下载文件 -> 读取文件内容 -> 本地业务处理。

其中从第三方sftp下载文件包含了，远程执行ls命令，过滤出符合条件的文件，复制文件内容到本地。

除了“大方法”外，代码和逻辑本身不够精简，不足够的易懂，也导致测试困难。
